---
applyTo: "app/**/*.{ts,tsx},server/**/*.{ts,tsx}"
---
# Frontend development (inngar)

- Use React Router patterns already present in the repo.
- Keep route logic close to route files; avoid unnecessary global state.
- Prefer `@navikt/ds-react` and existing Aksel patterns.
- Keep components focused and composable.
