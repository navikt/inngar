# Copilot instructions for inngar

- Prefer existing patterns in `app/` and `server/` before introducing new abstractions.
- Keep TypeScript strict and avoid `any` unless absolutely necessary.
- Use Aksel components/tokens over ad-hoc UI implementations.
- Preserve route behavior and API contracts unless explicitly asked to change them.
- For behavior changes, add or update tests where practical.
